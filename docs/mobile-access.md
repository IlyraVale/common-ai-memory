# Mobile Access

The safe default is no mobile exposure. For a private LAN deployment, bind to a deliberately selected interface, restrict the firewall to trusted devices, and use an authenticated HTTPS reverse proxy. For access outside the LAN, choose a maintained private-network or zero-trust product and follow its official setup guide.

Private network addresses, device identifiers, tunnel identifiers, and instance configuration do not belong in this repository.

